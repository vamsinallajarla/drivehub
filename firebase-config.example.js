// firebase-config.example.js
// Template for firebase-config.js — this file IS meant to be committed.
//
// Setup:
//   1. Copy this file and rename the copy to  firebase-config.js
//   2. Fill in your real values from:
//      Firebase console → Project settings (⚙️) → General → Your apps
//   3. firebase-config.js is already listed in .gitignore, so your real
//      values won't be committed.

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
